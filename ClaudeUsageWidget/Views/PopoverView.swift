import SwiftUI

struct PopoverView: View {
    @Environment(AppViewModel.self) private var viewModel

    var body: some View {
        VStack(spacing: 0) {
            // Placeholder — full layout implemented in Stories 1.3, 1.4
        }
        .frame(width: 280)
        .padding(.horizontal, 16)
        .padding(.vertical, 12)
    }
}
